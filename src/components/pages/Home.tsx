import { Main } from '../../configs/utilities'

const Home = () => {
  // Parse List function
  return <Main></Main>
}

export default Home

// if(!token) {
//   logon()
// }
// The Home Page - Shows the list of available rundowns to choose from
// Grabbing list again here is probably redunant, probably need a way to grab the list data as a string instead?
// const { data: list } = useGetLists()

// if (list) {
//   console.log('List: ', list)
// } else {
//   console.log('No list.')
// }
// const listResponse: String = JSON.stringify(list)
